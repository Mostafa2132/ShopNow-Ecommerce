# 🛒 متجر إلكتروني متكامل (E-Commerce Platform)

منصة تجارة إلكترونية حديثة وسريعة مبنية باستخدام أحدث تقنيات الويب **Next.js 16 (App Router)**، مصممة لتوفير تجربة مستخدم سلسة وتفاعلية.

## ✨ المميزات الرئيسية (Key Features)

*   **تصفح المنتجات:** عرض المنتجات، الأقسام (Categories)، الماركات (Brands)، والصفقات اليومية (Deals).
*   **نظام المصادقة (Authentication):** تسجيل الدخول، إنشاء حساب، استعادة كلمة المرور، وإدارة الجلسات باستخدام (Cookies).
*   **إدارة الحساب الشخصي (User Profile):** تحديث بيانات المستخدم، تغيير كلمة المرور، وإدارة عناوين الشحن.
*   **عربة التسوق والطلبات (Cart & Orders):** إضافة المنتجات للعربة، إتمام عملية الشراء (Checkout)، ومتابعة الطلبات السابقة (All Orders).
*   **المفضلة (Wishlist):** إضافة المنتجات المفضلة للرجوع إليها لاحقاً.
*   **التقييمات والمراجعات (Reviews):** إمكانية تقييم المنتجات وقراءة مراجعات المستخدمين.
*   **أداء عالي و SEO:** تحسين محركات البحث مع دعم لـ Sitemap، وإدارة صفحات الخطأ (404 Not Found & Error pages) لتجربة مستخدم أفضل.
*   **حركات وتفاعلات سلسة (Animations):** واجهات تفاعلية جذابة باستخدام Framer Motion و Sliders متقدمة.

---

## 💻 التقنيات المستخدمة (Tech Stack)

### ⚙️ الإطار الأساسي (Core)
*   **[Next.js](https://nextjs.org/)** (v16.1.6) - إطار عمل React لتقديم الصفحات من الخادم (SSR).
*   **[React](https://react.dev/)** (v19) - لبناء واجهات المستخدم.

### 🎨 التصميم والواجهات (Styling & UI)
*   **[Tailwind CSS](https://tailwindcss.com/)** (v4) - لتنسيق العناصر بمرونة وسرعة.
*   **[Framer Motion](https://www.framer.com/motion/)** - لإنشاء حركات (Animations) سلسة للمكونات.
*   **Carousels & Sliders:** تم استخدام (`Swiper`، `@splidejs/react-splide`، `react-fast-marquee`) لعرض المنتجات والعروض بشكل متحرك.
*   **[React Icons](https://react-icons.github.io/react-icons/)** - مكتبة الأيقونات الشاملة.
*   **[React Toastify](https://fkhadra.github.io/react-toastify/)** - لإظهار الإشعارات والتنبيهات المنبثقة.

### 🗄️ إدارة الحالة وجلب البيانات (State Management & Fetching)
*   **[Redux Toolkit](https://redux-toolkit.js.org/)** (`react-redux`) - لإدارة الحالة العامة للتطبيق (Global State) مثل سلة التسوق.
*   **[React Query](https://tanstack.com/query/latest)** (`@tanstack/react-query`) - لإدارة حالة البيانات القادمة من الخادم (Server State)، الكاشينج (Caching)، والمزامنة.
*   **[Axios](https://axios-http.com/)** - كعميل HTTP للتواصل مع الـ API الخارجي.

### 📝 النماذج والتحقق (Forms & Validation)
*   **[Formik](https://formik.org/)** - لإدارة حالة النماذج (Forms).
*   **[Yup](https://github.com/jquense/yup)** - للتحقق من صحة البيانات المدخلة (Validation Rules).
*   **React Password Strength Bar** - لمؤشر قوة كلمة المرور أثناء التسجيل.

### 🍪 أدوات مساعدة أخرى (Utilities)
*   **[cookies-next](https://www.npmjs.com/package/cookies-next)** - لإدارة ملفات تعريف الارتباط (Cookies) الخاصة بالمصادقة من جهة العميل والخادم.

---

## 📂 هيكل المشروع (Project Structure)

المشروع مبني باستخدام **Next.js App Router**:

```text
├── src/
│   ├── app/                 # يحتوي على جميع مسارات وصفحات التطبيق (Routing)
│   │   ├── (auth)/          # مسارات المصادقة (تسجيل، دخول، استرجاع كلمة المرور)
│   │   ├── (products)/      # مسارات عرض المنتجات
│   │   ├── (profile)/       # مسارات لوحة تحكم المستخدم وإعداداته
│   │   ├── cart/            # صفحة عربة التسوق
│   │   ├── check-out/       # إتمام عملية الدفع والشراء
│   │   ├── wishlist/        # صفحة المنتجات المفضلة
│   │   ├── allorders/       # تاريخ الطلبات
│   │   └── globals.css      # التنسيقات العامة للمشروع
│   ├── Components/          # المكونات القابلة لإعادة الاستخدام (أزرار، حقول إدخال، الخ...)
│   ├── store/               # إعدادات Redux Toolkit وإدارة الحالة
│   ├── hooks/               # Custom React Hooks
│   └── lib/                 # دوال الإعداد والمساعدة (Axios config, etc.)
├── public/                  # الصور والملفات الثابتة
└── package.json             # الاعتمادات والسكربتات
```

---

## 🚀 كيفية تشغيل المشروع محلياً (Installation & Setup)

1. **نسخ مستودع المشروع (Clone the repository)**
   ```bash
   git clone <repository-url>
   cd e-commerce
   ```

2. **تثبيت الحزم والاعتمادات (Install dependencies)**
   ```bash
   npm install
   # أو باستخدام yarn
   yarn install
   ```

3. **إعداد متغيرات البيئة (Environment Variables)**
   قم بإنشاء ملف `.env.local` في الجذر الرئيسي للمشروع وأضف المتغيرات الأساسية (مثل روابط الـ API).
   ```env
   NEXT_PUBLIC_BASE_URL=https://your-api-url.com/api
   ```

4. **تشغيل خادم التطوير (Start development server)**
   ```bash
   npm run dev
   # أو
   yarn dev
   ```
   سيتم تشغيل التطبيق على الرابط `http://localhost:3000`.

---

## 📜 الأوامر المتاحة (Scripts)

*   `npm run dev`: تشغيل المشروع في وضع التطوير.
*   `npm run build`: بناء وتجهيز المشروع لبيئة الإنتاج (Production).
*   `npm run start`: تشغيل مشروع الـ Production بعد عملية البناء.
*   `npm run lint`: فحص وتنسيق الأكواد باستخدام ESLint.

---

## 📄 الترخيص (License)
هذا المشروع متاح للاستخدام الخاص والمطورين. (يمكنك تعديل هذا القسم لإضافة رخصة مثل MIT إذا رغبت).
